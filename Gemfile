# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem 'nokogiri'
gem 'rack', '~> 2.2.6'
gem 'rspec'
gem "github-pages", "~> 206", group: :jekyll_plugins
gem 'wdm', '>= 0.1.0' if Gem.win_platform?
# gem "kramdown", ">= 2.3.0" #  cant update due to dependency ...github-pages (~> 206) x64-mingw32 was resolved to 206, which depends on kramdown (= 1.17.0) x64-mingw32